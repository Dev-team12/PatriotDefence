package defencer.controller;

/**
 * @author Igor Hnes on 14.05.17.
 */
public class DateTest {

    /*public static void main(String[] args) {


        final LocalDate beforeDayOff = LocalDate.of(2017, 6, 27);
        final LocalDate afterDayOff = LocalDate.of(2017, 6, 29);

        final LocalDate projectStart = LocalDate.of(2017, 5, 30); // ok
        final LocalDate projectFinish = LocalDate.of(2017, 6, 26); // ok

        if ((projectStart.isBefore(beforeDayOff) || projectStart.isAfter(afterDayOff))
                && (projectFinish.isBefore(beforeDayOff) || projectFinish.isAfter(afterDayOff))) {
            System.out.println("Status free");
        } else {
            System.out.println("Status busy");
        }

        if ((projectStart.isBefore(beforeDayOff)
                || projectStart.isAfter(afterDayOff))
                && (projectFinish.isBefore(beforeDayOff)
                || projectFinish.isAfter(afterDayOff))
                && (!beforeDayOff.isAfter(projectStart)
                || !beforeDayOff.isBefore(projectFinish))) {
            System.out.println("Status free");
        } else {
            System.out.println("Status busy");
        }
    }*/
}
